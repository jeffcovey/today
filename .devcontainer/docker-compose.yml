version: '3.8'
services:
  notion-cli:
    volumes:
      # Mount the workspace folder to the expected location for VS Code
      - ..:/workspaces/notion-cli:cached
    
    # Override entrypoint and command so container stays running for development
    entrypoint: []
    command: ["sleep", "infinity"]
