#!/usr/bin/env node

/**
 * bin/sync - DEPRECATED
 *
 * This script has been decomposed. All functionality has been moved to:
 *
 * Already migrated to plugins:
 * - Email sync → imap-email plugin
 * - Calendar sync → google-calendar, public-calendars plugins
 *
 * Extracted to tmp/ with GitHub issues for migration:
 * - tmp/plan-cleanup.js (#73) - Plan file cleanup & AI summaries
 * - tmp/inbox-processing.js (#74) - Inbox file processing
 * - tmp/pobox-sync.js (#69) - Pobox sent mail (was broken)
 * - tmp/time-blocking-cleanup.js (#70) - Time blocking calendar cleanup
 * - tmp/time-tracking-calendar.js (#71) - Time tracking to calendar
 * - tmp/contacts-sync.js (#72) - iCloud contacts sync
 *
 * Thin wrappers that can be called directly:
 * - Database health → node -e "import('../src/db-health.js').then(m => m.ensureHealthyDatabase())"
 * - Plugin sync → bin/plugins sync
 * - Task cache → bin/tasks update-cache
 * - Topic files → bin/tasks generate-topics
 * - Task classification → bin/tasks add-date-added && bin/tasks classify-stages && bin/tasks add-topics
 *
 * To run the old full sync workflow, use these commands in sequence:
 *   bin/plugins sync
 *   node tmp/plan-cleanup.js clean-tasks
 *   node tmp/plan-cleanup.js cancel-old
 *   node tmp/inbox-processing.js
 *   bin/tasks update-cache
 *   bin/tasks generate-topics
 */

console.log('bin/sync is deprecated.');
console.log('');
console.log('Use these commands instead:');
console.log('  bin/plugins sync              # Sync all enabled plugins');
console.log('  node tmp/plan-cleanup.js      # Plan file cleanup');
console.log('  node tmp/inbox-processing.js  # Process inbox');
console.log('  bin/tasks update-cache        # Update task cache');
console.log('  bin/tasks generate-topics     # Generate topic files');
console.log('');
console.log('See GitHub issues #69-74 for migration status.');

process.exit(0);
